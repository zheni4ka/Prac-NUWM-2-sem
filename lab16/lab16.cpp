#include <fstream> 
#include <iostream> 
using namespace std;
int main() {

    //16.1
    /*const string filename = "f.txt";
    ofstream outputFile(filename);

    if (!outputFile.is_open()) {
        cerr << "Error: Could not open the file " << filename << endl;
        return 1;
    }

    for (int i = 1; i <= 9; ++i) {
        double Ai = 1.5 * i;
        outputFile << Ai << endl; 
    }

    outputFile.close();

    cout << "File '" << filename << "' created successfully with 9 components." << endl;*/
}